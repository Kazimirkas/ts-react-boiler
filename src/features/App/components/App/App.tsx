import * as React from "react";

export default class App extends React.Component <{}> {
        
        render() {
                return (
                        <div>
                               I'm a super app component
                        </div>
                );
        }
};
